import React, { Component, PropTypes } from 'react';

class DateFilterCollection extends Component {
    render() {
        return (
            <div className="date-filter-collection">
            </div>
        );
    }
}

DateFilterCollection.propTypes = {
};

export default DateFilterCollection;
